{% extends "../_form.njk" %}
{% set title = 'Arrange an appointment in another location' %}
{% set backLink = ('/case/' + crn + '/arrange-appointment/' + id + '/attendance') if noLocations else ('/case/' + crn + '/arrange-appointment/' + id + '/location')  %}
{% set notStandardForm = true %}
{% block primary %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h2 class="govuk-heading-l" data-qa="pageHeading">{{ title }}</h2>
      <p>You can only arrange appointments from a list of locations associated with the probation practitioner’s team.</p>
      <p>You’ll need to open {{case.name.forename}}’s case on NDelius to arrange an appointment in another location.</p>
      <p>
        <a href="{{ deliusDeepLinkUrl('ContactList', crn) }}" target="_blank">Continue on NDelius</a> (opens in new tab)</p>
      <p>
        <a href="{{ backLink }}">Cancel and return to previous screen</a>
      </p>
    </div>
  </div>
{% endblock %}